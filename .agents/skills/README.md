# Ringo Skills

This folder contains reusable skills for agents working with `stream_kernel` (`ringo`).

Available skills:

- `create-runtime-component`
- `add-resource-adapter`
- `wire-control-plane-node`
- `add-routing-rule`

Each skill is a standalone `*.SKILL.md` file in this folder.
